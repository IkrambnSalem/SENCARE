import { MyfilterUserPipe } from './myfilter-user.pipe';

describe('MyfilterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new MyfilterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
